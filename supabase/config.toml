# Minimal Supabase CLI configuration for the version-controlled schema.
# Keep environment-specific credentials out of this file.
project_id = "pm-agent-v"

[db]
major_version = 17

[db.migrations]
enabled = true

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]
